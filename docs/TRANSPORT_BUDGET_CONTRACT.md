# Transport budget contract

The transport budget separates raw contact capacity from protocol overhead, expected loss, and prioritized evidence volumes: health, candidate/context, background, and forensic bursts. A shortfall is explicit rather than silently discarding evidence.

This is arithmetic only, not RF-link validation. Real feasibility requires contact analysis, modulation/coding, encryption overhead, FEC/retry behavior, storage limits, fault cases, and representative hardware tests.
