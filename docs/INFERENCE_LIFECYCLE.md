# Inference-hypothesis lifecycle

Associated candidates may become a research inference hypothesis only through an explicit, evidence-backed transition. A hypothesis may be inferred, rejected, retracted, or archived; terminal records cannot return to inference. Every transition requires evidence references and a rationale.

This lifecycle is deliberately not an object catalog, debris track, collision assessment, release workflow, or command authority. Retraction remains a first-class outcome.
