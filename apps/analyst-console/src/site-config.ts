/**
 * The only project-specific presentation settings for this console.
 *
 * Copy the console, change these values and the document metadata in
 * `index.html`, then regenerate `public/research-status.json` from the new
 * project's authoritative registries.  Do not use this file to make a claim
 * that is not supported by the snapshot.
 */
export const siteConfig = {
  projectName: "Project HEIMDALL ELECTRA",
  productName: "Research Evidence Console",
  shortDescription: "A read-only research status and evidence-boundary console.",
  supportPath: "/",
} as const;
